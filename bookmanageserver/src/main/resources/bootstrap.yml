spring:
  application:
    name: book-manage-server                     #指定了配置文件的应用名
  cloud:
    config:
      uri: http://localhost:8004        #Config server的uri
      profile: dev                       #指定的环境
      label: master                      #指定分支
      #fail-fast: true
      #discovery:
        #enabled: true
        #service-id: book-config-server
server:
  port: 8005

